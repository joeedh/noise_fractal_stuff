import {describe, it, expect} from 'vitest'

/* Placeholder smoke test so `pnpm test` has something to run until real
 * unit tests are added. */
describe('smoke', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
